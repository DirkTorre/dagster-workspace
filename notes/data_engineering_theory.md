3 stages of data:

1. raw: where files are downloaded and not worked with in any way
2. staging: data is transformed (or not) and put into a database
3. production: data that is ready to be used

querying the database:
    we use dbt for this, because it has more options.
